{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "SylvaNote.Client (Debug)",
            "type": "coreclr",
            "request": "launch",
            "preLaunchTask": "dev: frontend + build",
            "program": "${workspaceFolder}/bin/Debug/net10.0/SylvaNote.Client.dll",
            "args": [],
            "cwd": "${workspaceFolder}",
            "stopAtEntry": false,
            "console": "internalConsole"
        }
    ]
}
